//多表查询字段。
//传入参数形式如：
//moreTableSelect(array("zy_zymember"=>array("id", "userid"), "zy_zycoupon"=>array("*")), array("userid","shopid"), "userid=1")
//2019/4/18补充大于等于3张表连接的时候on来left join,第一个个参数是主表和附表1参数连接，第二个参数是主表和附表2参数连接的桥梁，以此类推。
//$where暂时只支持sql语句，以后再修改成数组类型，当B1,B2,B3都有同样参数的时候时，需要区别
//2019/4/25$where 支持数组传递了
//$returnArray == 1当只有一条数据的时候也返回数组形式，=0的时候只返回数据形式
